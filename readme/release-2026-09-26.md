# 2026-09-26 发布记录

发布范围：当前工作区后端修复、前四项审查修复及已构建前端。两个生产域名仍使用原 Worker Routes，GreenCloud 保留独立原站。没有修改已有图片对象、账户密码或存储渠道凭证。

- 前端入口：app.ce32de7e.js。
- VPS 镜像：cloudflare-imgbed:release-20260926-final。
- VPS 发布目录：/opt/cloudflare-imgbed/releases/release-20260926-final。
- 共享数据库已原生导出备份；服务器完整配置/数据备份：/opt/backups/cloudflare-imgbed/20260925T185949Z（UTC）。
- 现有 D1 新增字段及查询索引由兼容升级路径处理，HF 提交协调器已绑定。
- 回归：138 项通过（31 + 25 + 82）。线上验证使用临时诊断会话和新建测试文件，不代表实际输入管理员密码测试。
- 线上验证：两个域名与 VPS 的授权列表、跨后端上传读取、Range、重命名旧别名、测试文件删除；HF 协调器仅测试就绪，不向 HF/TG 发送测试媒体。

发布检查额外修复：遥测不再假定重建 Request 带 cf，遥测准备/清理错误不会阻断或重放上传；不再向遥测记录请求 Cookie/Authorization。共享网关内联备份的保守预算本部署设为 32 MiB（默认 16 MiB、硬上限 64 MiB），维持同一快照预算检查。数据库进一步增长超过预算时应使用原生 D1 导出。

回滚：本次最终容器可通过恢复 /opt/cloudflare-imgbed/compose.yaml.before-release-20260926-final 后执行 docker compose up -d --no-deps imgbed 回到同日上一镜像。Worker 回退必须选择支持当前版本化索引和 D1 字段的同日版本；不要直接回退到旧索引格式版本，也不要还原数据库覆盖发布后上传。既有容器镜像与备份保留。

发布后备份回归：备份仅包含当前索引 generation，跳过已失效、可重建的旧分块缓存；文件、设置与当前索引仍在同一快照中导出，未删除线上记录。新增备份覆盖测试通过。
